let Koa,Router,bodyParser,mongoose,api;_8a7‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_8a7‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_8a7‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_8a7‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_8a7‍.w("./api",[["default",["api"],function(v){api=v}]]);require('dotenv').config();







const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI, { useNewUrlParser: true })
  .then(() => {
    console.log('Connected to MongoDB');
  })
  .catch((e) => {
    _8a7‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/api', api.routes()); // api 라우트 적용

// 라우터 적용 전에 bodyParser 적용
app.use(bodyParser());

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
  _8a7‍.g.console.log('Listening to port %d', port);
});
