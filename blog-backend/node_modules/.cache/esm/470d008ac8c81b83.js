let Koa,Router,bodyParser,mongoose,api,jwtMiddleware;_c8c‍.w("koa",[["default",["Koa"],function(v){Koa=v}]]);_c8c‍.w("koa-router",[["default",["Router"],function(v){Router=v}]]);_c8c‍.w("koa-bodyparser",[["default",["bodyParser"],function(v){bodyParser=v}]]);_c8c‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_c8c‍.w("./api",[["default",["api"],function(v){api=v}]]);_c8c‍.w("./lib/jwtMiddleware",[["default",["jwtMiddleware"],function(v){jwtMiddleware=v}]]);require('dotenv').config();








const { PORT, MONGO_URI } = process.env;

mongoose
  .connect(MONGO_URI, { useNewUrlParser: true })
  .then(() => {
    console.log('Connected to MongoDB');
  })
  .catch((e) => {
    _c8c‍.g.console.error(e);
  });

const app = new Koa();
const router = new Router();

// 라우터 설정
router.use('/api', api.routes()); // api 라우트 적용

// 라우터 적용 전에 bodyParser 적용
app.use(bodyParser());
app.use(jwtMiddleware);

// app 인스턴스에 라우터 적용
app.use(router.routes()).use(router.allowedMethods());

const port = PORT || 4000;
app.listen(port, () => {
  _c8c‍.g.console.log('Listening to port %d', port);
});
